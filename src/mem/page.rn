pub const PAGE_SIZE: usize = 4096;

pub fn padding_up(value: usize, align: usize) usize {
    if (align == 0) {
        return value;
    }

    let mask = align - 1usize;
    return (value + mask) & ~mask;
}
