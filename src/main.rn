mod boot;

#[export_name("_start")]
fn kmain() void {

    let response = boot.framebuffer_request.response;

    for(;;) {
        @asm(.{
            asm: "hlt",
            volatile: true
        });
    }
}
